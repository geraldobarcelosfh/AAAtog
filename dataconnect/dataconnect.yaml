specVersion: "v1"
serviceId: "tog-final"
location: "us-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "togfinal-datab"
      cloudSql:
        instanceId: "studio-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
